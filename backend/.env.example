# ============================================================
# 后端本地开发环境配置示例
# 使用方式：复制本文件为 backend/.env，并按本机环境修改配置值。
# 注意：backend/.env 包含真实密钥和本地连接信息，禁止提交到 Git。
# ============================================================

# =========================
# 基础服务配置
# =========================

# 应用名称，用于日志、监控和接口文档展示。
APP_NAME=AI 多智能体平台

# 当前运行环境，本地开发固定使用 development。
APP_ENV=development

# 是否开启调试模式，本地开发可设为 true，生产环境必须为 false。
DEBUG=true

# 后端服务监听地址，本地开发通常使用 127.0.0.1。
SERVER_HOST=127.0.0.1

# 后端服务监听端口。
SERVER_PORT=8000

# API 版本前缀，用于统一管理后端接口路径。
API_V1_PREFIX=/api/v1

# 允许跨域访问的前端地址，多个地址用英文逗号分隔。
CORS_ALLOWED_ORIGINS=http://127.0.0.1:5173,http://localhost:5173

# =========================
# 安全与认证配置
# =========================

# 后端签名密钥，用于 JWT、会话签名等安全场景；本地可使用占位值，生产必须使用强随机值。
SECRET_KEY=dev-change-me

# JWT 访问令牌有效期，单位为分钟。
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# JWT 刷新令牌有效期，单位为分钟。
REFRESH_TOKEN_EXPIRE_MINUTES=43200

# 密码哈希算法配置，默认使用 argon2。
PASSWORD_HASH_ALGORITHM=argon2

# =========================
# MySQL 数据库配置
# =========================

# MySQL 数据库连接地址，建议本地开发使用独立开发库。
DATABASE_URL=mysql+pymysql://ai_agent:ai_agent_password@127.0.0.1:3306/ai_agent_dev?charset=utf8mb4

# 数据库连接池大小，用于控制常驻连接数量。
DATABASE_POOL_SIZE=10

# 数据库连接池额外溢出连接数量。
DATABASE_MAX_OVERFLOW=20

# 是否输出 SQL 调试日志，本地排查问题时可设为 true。
DATABASE_ECHO=false

# =========================
# Redis 配置
# =========================

# Redis 连接地址，用于缓存、分布式锁、任务状态和事件中转。
REDIS_URL=redis://127.0.0.1:6379/0

# Redis key 前缀，用于区分不同项目或环境的数据。
REDIS_KEY_PREFIX=ai_agent_dev

# =========================
# 异步任务配置
# =========================

# 任务队列 broker 地址，默认使用 Redis。
CELERY_BROKER_URL=redis://127.0.0.1:6379/1

# 任务结果存储地址，默认使用 Redis。
CELERY_RESULT_BACKEND=redis://127.0.0.1:6379/2

# 单个智能体任务默认超时时间，单位为秒。
AGENT_TASK_TIMEOUT_SECONDS=300

# 单个智能体运行最大步骤数，用于防止无限循环。
AGENT_MAX_STEPS=20

# =========================
# 大模型与 LangChain 配置
# =========================

# 默认模型提供商，例如 openai、anthropic、deepseek、qwen、openrouter、ollama。
DEFAULT_LLM_PROVIDER=openrouter

# 默认聊天模型名称。
DEFAULT_CHAT_MODEL=openai/gpt-4.1-mini

# 默认推理模型名称，用于规划、审核和复杂推理。
DEFAULT_REASONING_MODEL=openai/gpt-4.1

# OpenAI API Key，本地开发按需填写；不要提交真实值。
OPENAI_API_KEY=

# Anthropic API Key，本地开发按需填写；不要提交真实值。
ANTHROPIC_API_KEY=

# DeepSeek API Key，本地开发按需填写；不要提交真实值。
DEEPSEEK_API_KEY=

# OpenRouter API Key，本地开发按需填写；不要提交真实值。
OPENROUTER_API_KEY=

# 本地 Ollama 服务地址，用于接入本地模型。
OLLAMA_BASE_URL=http://127.0.0.1:11434

# =========================
# 向量数据库配置
# =========================

# 向量数据库提供商，本地 MVP 推荐 qdrant。
VECTOR_STORE_PROVIDER=qdrant

# Qdrant 服务地址。
QDRANT_URL=http://127.0.0.1:6333

# Qdrant API Key，本地无鉴权时可留空。
QDRANT_API_KEY=

# Qdrant collection 前缀，用于区分不同环境的数据。
QDRANT_COLLECTION_PREFIX=ai_agent_dev

# =========================
# 对象存储配置
# =========================

# 文件存储提供商，本地开发可使用 local，生产推荐 s3 或 minio。
STORAGE_PROVIDER=local

# 本地文件存储路径，用于保存上传文件、运行产物和生成的 PDF。
LOCAL_STORAGE_PATH=./storage

# S3 或 MinIO endpoint，本地使用 local 时可留空。
S3_ENDPOINT_URL=

# S3 或 MinIO bucket 名称，本地使用 local 时可留空。
S3_BUCKET_NAME=

# S3 或 MinIO access key，本地使用 local 时可留空。
S3_ACCESS_KEY_ID=

# S3 或 MinIO secret key，本地使用 local 时可留空。
S3_SECRET_ACCESS_KEY=

# =========================
# 工具与 MCP 配置
# =========================

# 是否启用 MCP 工具接入。
MCP_ENABLED=true

# MCP Server 配置目录，用于加载本地 MCP server 定义。
MCP_SERVER_CONFIG_DIR=./config/mcp-servers

# Firecrawl API Key，用于联网搜索、网页抓取和站点爬取。
FIRECRAWL_API_KEY=

# Tavily API Key，用于可选联网搜索工具。
TAVILY_API_KEY=

# Brave Search API Key，用于可选联网搜索工具。
BRAVE_SEARCH_API_KEY=

# 是否启用沙箱工具，例如终端、代码运行和浏览器自动化。
SANDBOX_ENABLED=true

# 沙箱默认运行超时时间，单位为秒。
SANDBOX_TIMEOUT_SECONDS=120

# =========================
# 可观测性配置
# =========================

# 是否启用 OpenTelemetry 链路追踪。
OTEL_ENABLED=false

# OpenTelemetry collector 地址。
OTEL_EXPORTER_OTLP_ENDPOINT=http://127.0.0.1:4318

# LangSmith API Key，用于 LangChain 调试、追踪和评测。
LANGSMITH_API_KEY=

# LangSmith 项目名称。
LANGSMITH_PROJECT=ai-agent-dev

# Langfuse 公钥，用于可选 LLM 可观测性。
LANGFUSE_PUBLIC_KEY=

# Langfuse 私钥，用于可选 LLM 可观测性。
LANGFUSE_SECRET_KEY=

# Langfuse 服务地址。
LANGFUSE_HOST=
